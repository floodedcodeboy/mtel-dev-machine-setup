
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
# Installed nvm via Ansible START ANSIBLE MANAGED BLOCK
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" # This loads nvm
# Installed nvm autocomplete via Ansible START ANSIBLE MANAGED BLOCK
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
# Ansible END ANSIBLE MANAGED BLOCK
# Ansible END ANSIBLE MANAGED BLOCK
